import { EmailAvailabilityDirective } from './email-availability.directive';

describe('EmailAvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailAvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
