import { RetypePasswordDirective } from './retype-password.directive';

describe('RetypePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RetypePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
